<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Wordle Stats</title>
    <link rel="stylesheet" href="css/style.css">
    <script src="js/main.js" defer></script>
</head>

<body>
<!-- To do: Separate guesses column out into me, Fet, winner
    Make table sortable by click on Number or guesses-->
<%- include('partials/nav.ejs') %>
<table>
    <thead>
        <tr>
            <th>Number</th>
            <th class="teal">Christina</th>
            <th class="fab">Fet</th>
            <th>Solution</th>
            <th>Guesses</th>
        </tr>
    </thead>
    <tbody>
    <% for (let entry of info) {%>
        <tr>
            <td class="number">
                <%= entry.num %>
            </td>
            <td class="me">
                <% for (let arr of entry.cStarter) {%>
                    <span class=<%= arr[1]%>><%= arr[0] %></span>
                <% } %>
            </td>
            <td class="fet">
                <% for (let arr of entry.fStarter) {%>
                    <span class=<%= arr[1]%>><%= arr[0] %></span>
                <% } %>
            </td>
            <td class="solution">
                <%= entry.solution %>
            </td>
            <td class="guesses">
                <span class=<%= entry.christina < entry.fet ? "teal" : entry.fet < entry.christina ? "fab" : ""%>>
                <%= entry.christina %> / <%= entry.fet %>
                </span>
            </td>
        </tr>
    <% } %>
    </tbody>

</table>


</body>